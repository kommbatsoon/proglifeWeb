# Guidelines to work with git

## Branches naming

Feature branch template:
```
feature/${ticketId}-context
```

Fix branch template:
```
fix/${ticketId}-context
```

Release branch template:
```
release/${version}
```
